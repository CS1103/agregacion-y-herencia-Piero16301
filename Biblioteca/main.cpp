#include <iostream>

#include "Biblioteca.h"
#include "Volumen.h"
#include "Revista.h"
#include "Libro.h"

#define CATCH_CONFIG_MAIN
#include "catch.hpp"

#ifdef CATCH_CONFIG_MAIN

/*int main() {
    Biblioteca biblioteca(0,0,20,20);
    Volumen* pointer1 = new Revista(1,"New York Times",1);
    Volumen* pointer2 = new Revista(2,"The Economist",2);
    Volumen* pointer3 = new Revista(3,"BBC News",3);
    Volumen* pointer4 = new Libro(4,"Four, the tech horses",1);
    Volumen* pointer5 = new Libro(5,"Origin",2);
    Volumen* pointer6 = new Libro(6,"The DaVinci Code",3);
    biblioteca.incluir(pointer1);
    biblioteca.incluir(pointer2);
    biblioteca.incluir(pointer3);
    biblioteca.incluir(pointer4);
    biblioteca.incluir(pointer5);
    biblioteca.incluir(pointer6);
    std::cout << "Numero de libros en biblioteca: " << biblioteca.numLibros << std::endl;
    std::cout << "Numero de revistas en biblioteca: " << biblioteca.numRevistas << std::endl;
    biblioteca.mostrarBiblioteca();
    return 0;
}*/

#endif